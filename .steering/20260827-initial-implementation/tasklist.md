# 初回実装 タスクリスト

作成日: 2026-08-27
参照元: `.steering/20260827-initial-implementation/design.md`
ステータス: ドラフト(要承認)

進捗の見方: `[ ]` 未着手 / `[~]` 着手中 / `[x]` 完了

---

## 0. 基盤構築

- [x] リポジトリ雛形作成(`docs/repository-structure.md` 準拠のディレクトリ構成。frontend/backend/shared/scripts/docker/storageを作成)
- [x] フロントエンド初期化(Vite + React + TypeScript + Tailwind CSS v4。モックアップのカラー/Murechoフォント/角丸トークンを`src/index.css`の`@theme`に反映。`npm run build`成功を確認)
- [x] バックエンド初期化(Node.js + TypeScript + Express。`src/modules/*`の11モジュールディレクトリを作成。`npm run build`/`npm run typecheck`成功を確認)
- [x] DB接続基盤のセットアップ(`mssql`ドライバで社内SQL Server 10.194.5.57への接続を実装。`/api/health/db`で実機接続を確認済み、要件定義書通りTLS証明書は`trustServerCertificate: true`が必要と判明)
- [ ] マイグレーション基盤(`backend/src/db/migrations/`はディレクトリのみ作成済み。実スキーマは1章で対応)
- [ ] CI設定(lint・型チェック・テストの自動実行。GitHub Actions等のワークフローは未作成)

**完了条件**: 空のフロントエンド/バックエンドアプリがローカルで起動し、CIが通る状態。→ ローカル起動・ビルド・DB疎通は確認済み。CI未設定のため本章は完了扱いにしない。

**メモ(2026-08-27)**: Vite 8(rolldown版)がこの開発機のNode.js v22.11.0(要件v20.19+/v22.12+)と噛み合わずネイティブバイナリエラーが発生したため、安定版のVite 6系に固定した。バックエンドはExpressを採用(Fastifyとの比較検討はせず、チームの学習コストの低さを優先)。DBアクセスはORMを使わず`mssql`(tedious)を直接使用する方針(`docs/architecture.md`で確定)。

## 1. DBスキーマ(01〜09マイグレーション)

- [x] マイグレーション実行の仕組み(`backend/src/db/migrate.ts`、`schema_migrations`テーブルで適用済み管理、`npm run migrate`)
- [x] 001_users_groups(app_user, app_group, user_group)
- [x] 002_board(post, post_comment, post_attachment、permalink_slug含む)
- [x] 003_calendar(calendar_event)
- [x] 004_holidays(holiday)
- [x] 005_top_page(top_page_setting, group_member_order)
- [x] 006_rooms(meeting_room, room_reservation)
- [x] 007_files(folder, file_item, file_version、permalink_slug含む)
- [x] 008_logs(operation_log)
- [x] 009_google_sync(google_calendar_link)

**完了条件**: `docs/functional-design.md` のER図と一致するスキーマがマイグレーションで再現できる。→ SQLファイル・型チェック・ビルドは完了。**実DBへの適用は未実施**(本プロジェクト専用の接続アカウントが未発行のため。発行後に`npm run migrate`で適用し、実データで動作確認する)。

## 2. 認証・利用者管理

- [x] ログイン画面・セッション/認証処理(express-session、Cookieベース。フロントに`LoginPage`・`AuthContext`・`RequireAuth`を実装)
- [x] パスワードのハッシュ化保存(bcrypt、再表示不可。ユニットテストで平文非保持・照合を検証)
- [x] 利用者CRUD(管理者限定): 氏名・メール・ログインID・社員番号・所属グループ(複数可)・権限区分。`/api/admin/users`+管理画面UI(`UsersPage`)
- [x] 退職処理(論理削除、ログイン禁止化。`status=retired`でログイン不可になることをユニットテストで確認)

**完了条件**: 管理者が利用者を登録・編集・退職処理でき、一般社員はログインのみ可能。パスワードがハッシュ化されていることをDB上で確認できる。→ ロジックはフェイクリポジトリによるユニットテスト(bcryptハッシュ化・ログイン可否・退職処理)で検証済み。**実SQL Serverでの最終確認は未実施**(接続アカウント未発行のため)。

**メモ(2026-08-27)**: リポジトリ層は`UserRepository`インターフェースで抽象化し、本番用の`MssqlUserRepository`とテスト用の`FakeUserRepository`を用意。DB未接続でもビジネスロジックを検証できる構成にした。また、非同期ルートハンドラでDBエラーが未捕捉のままプロセスをクラッシュさせる不具合を発見し、`asyncHandler`+`errorHandler`ミドルウェアで修正(実機のダミー認証情報での動作確認中に発見)。初回管理者作成用に`npm run seed:admin`を追加。フロントはVite開発サーバーで`/api`を`localhost:3001`へプロキシする構成とし、本番のIIS ARR構成とパスの扱いを一致させた。SPAルーティング(react-router-dom採用)に伴い`deploy/iis/frontend.web.config`(SPAフォールバック)・`backend.web.config`(ARRリバースプロキシ)のテンプレートを追加。

## 3. お知らせ・掲示板

- [x] 投稿CRUD(全社/部署別、リッチテキスト本文)。`/api/posts`+一覧・詳細・新規投稿画面
- [x] 投稿本文のサニタイズ処理(XSS対策、`sanitize-html`。scriptタグ・イベントハンドラ属性・javascript:リンクを除去するテストで確認)
- [x] コメント機能
- [x] 既読管理(`post_read`テーブルを新設。ER図に無かったため010マイグレーションとして追加)
- [x] 操作ログ基盤の実装(`operation_log`への記録。`OperationLogRepository`として汎用化し、ファイル共有でも再利用できる設計)
- [x] 一覧・トップ画面での抜粋表示(装飾保持。`excerptHtml`でタグを壊さず切り詰め、テストで検証)

**完了条件**: リッチテキストで投稿でき、意図しないスクリプトが実行されないこと(サニタイズテスト含む)を確認。→ ユニットテスト(サニタイズ4件・抜粋3件・PostService7件、計14件)ですべて確認済み。**実SQL Serverでの最終確認は未実施**。

**メモ(2026-08-28)**: リッチテキストエディタは`document.execCommand`(太字・文字色のみ)を使った最小実装とした。画像埋め込みは要件未確定のためスコープ外。部署別投稿の部署選択は、利用者管理の所属グループAPIが未実装のため、暫定的に数値入力(部署ID直接指定)としている(グループ管理機能の実装時に選択式へ差し替えが必要)。固定リンクは`/board/link/:slug`で解決する経路を実装したが、掲示板詳細のみでファイル共有側は未実装(タスク8で対応)。

## 4. カレンダー(個人予定)

- [x] 予定CRUD(繰り返し予定含む。繰り返しはdaily/weekly/monthlyの単純間隔のみ対応。RFC5545 RRULE相当の複雑な例外指定は対象外というスコープ判断)
- [x] 公開対象(自分/全員)の実装
- [x] 非表示チェック(独立フラグ)の実装
- [x] 他者予定の閲覧権限ルールの適用(本人以外は編集・削除不可。403を返す)

**完了条件**: 公開対象と非表示の組み合わせが `functional-design.md` の表通りに動作する。→ ユニットテスト(occurrences 4件、CalendarService 6件)で3パターン(自分/全員+非表示OFF/全員+非表示ON)すべて確認済み。フロントに簡易的な個人予定ページ(`/calendar`)を実装し、作成・一覧・削除を手動確認可能な状態にした。**実SQL Serverでの最終確認は未実施**。

**メモ(2026-08-28)**: 権限エラー(他人の予定を編集/削除しようとした場合)が一律500になっていた問題を`HttpError`クラス導入で解消し、403を正しく返すよう修正(`errorHandler`を拡張)。フロントの`/calendar`は本日起点2週間のシンプルな一覧+登録フォームで、週間ガントチャート形式のUIは6章(トップ画面)で実装する。

## 5. 共通休日設定

- [x] 祝日管理画面(年度選択、一覧表示、追加・削除。管理者限定、`/admin/holidays`)
- [x] 会社休日(カレンダー機能の「全員」予定として誰でも登録可能。`event_type='company_holiday'`で公開対象・非表示・繰り返しをサーバー側で固定)
- [x] カレンダー画面での祝日+会社休日の重畳表示(`/calendar`で祝日・会社休日・個人予定を統合したタイムライン表示)

**完了条件**: 管理者が祝日を年次登録でき、誰でも会社休日を登録でき、両方がカレンダー上に表示される。→ ユニットテスト(HolidayService 4件、会社休日関連のCalendarService 4件)で確認済み。**実SQL Serverでの最終確認は未実施**。

**メモ(2026-08-28)**: 会社休日は「誰でも入力できるが削除・修正は入力者本人か管理者のみ」という要件定義書の推奨(3-2章)を採用し、`CalendarService`に権限判定ロジックを追加した(通常の個人予定は本人のみ、会社休日は本人または管理者)。祝日は日付の重複登録を`HttpError(409)`で防止。祝日データの外部取込み(内閣府データ等)は未確定事項のため、今回は手動登録のみ実装。

## 6. トップ画面

- [x] 週間ガントチャート(本日起点1週間、本人行+グループメンバー行。`/api/top-page/week-gantt`)
- [x] 表示グループ切替プルダウン(自分の所属グループを既定表示、`/api/groups/mine`)
- [x] ナビゲーション5ボタン(前週/前日/本日/翌日/翌週)、画面内一時状態としての保持(React state、画面遷移・リロードで自然にリセット)
- [x] 各利用者行下の「月表示」ボタン→個人カレンダー月表示画面への遷移(`/calendar/month/:userId`を新規実装)
- [x] 掲示板ウィジェット(新規登録ボタン、もっと見るリンク、表示件数設定を反映)。**ファイルウィジェットは未実装のプレースホルダー**(タスク8のファイル共有実装後に差し替え)

**完了条件**: `docs/mockups/casemax_mockup_top.html` の挙動(ナビゲーション、表示グループ切替、新規登録導線)が実データで再現される。→ バックエンドロジック(TopPageService)はユニットテスト5件で検証済み。フロントはビルド・lint通過、dev server起動確認済み。**実SQL Server・実ブラウザでのクリック動作確認は未実施**(DB接続情報未発行、ログインできないため)。

## 7. 会議室予約

- [x] 会議室マスタ(MEETING_ROOM。管理者が`/rooms`から追加可能)
- [x] 予約CRUD、空き状況表示(会議室ごとの予約一覧)
- [x] 二重予約防止ロジック(SERIALIZABLE分離レベル+UPDLOCK,HOLDLOCKで同時リクエストのレースコンディションも防止)
- [x] カレンダー予定との連動表示(予約作成時に予約者のカレンダーへ`[会議室名] タイトル`の予定を自動作成し、取消・変更にも追従)

**完了条件**: 同一会議室・同一時間帯への重複予約がエラーになる。→ ユニットテスト7件(重複検出、非重複時の許可、別会議室の並行予約、カレンダー連動作成・削除、権限制御)ですべて検証済み。**実SQL Serverでの最終確認(実際のトランザクション分離レベルの挙動含む)は未実施**。

**メモ(2026-08-28)**: `room_reservation`にタイトル列が無かったため011マイグレーションで追加(要件上「何のための予約か」を表示する必要があるため)。予約の変更・取消は予約者本人または管理者のみに制限(カレンダー予定の権限モデルと統一)。

## 8. ファイル共有

- [x] フォルダ階層管理(`/files`でフォルダ作成・パンくずナビゲーション)
- [x] アップロード/ダウンロード(multerで受信後、`env.storageDir`配下へ移動しDBにはメタデータのみ保持)
- [x] バージョン管理(再アップロードで新バージョンを追加、旧バージョンの実体も保持)
- [x] 操作ログへの記録(3章のOperationLogRepositoryを再利用、作成・更新・削除を記録)
- [x] トップ画面ウィジェットとの連携(新規登録導線をトップ画面「ファイル共有」ウィジェットに実装。プレースホルダーから実データに差し替え)

**完了条件**: 全社員がファイルを閲覧・編集・削除でき、操作ログに記録される。ゴミ箱機能が存在しないことを確認。→ ユニットテスト5件(アップロード・存在しないフォルダへのエラー・バージョン追加・削除時の実体ファイル削除・即時削除でゴミ箱がないこと)で実ファイルシステムを使って検証済み。**実SQL Serverでの最終確認は未実施**。

**メモ(2026-08-29)**: アクセス権限・ゴミ箱機能は要件通り実装しない(全社員が閲覧・編集・削除可能、削除は即時かつ不可逆)。ファイル実体の一時保存先は`storage/.tmp/`とし、確定後に`storage/`直下へ移動する2段階アップロードにした(multerの受信先と最終保存先を同一ボリュームにすることでrename操作をアトミックにする狙い)。固定リンク(permalink_slug)はDB制約上ここで生成しているが、ボタンUIと未ログイン時のリダイレクトは9章で対応する。

## 9. 固定リンク

- [x] 掲示板詳細画面への固定リンクボタン・URL表示・コピー機能(3章で実装済み、`/board/link/:slug`)
- [x] ファイル詳細画面への固定リンクボタン・URL表示・コピー機能(`FileDetailPage`新設、`/files/link/:slug`)
- [x] 未ログイン時にリンクを開いた場合、ログイン後に目的画面へ自動遷移

**完了条件**: 固定リンクをコピーし、未ログイン状態のブラウザで開くとログイン画面経由で目的画面に到達する。→ ロジック確認済み(下記メモ参照)。**実ブラウザでの一連の動作確認は未実施**(DB未接続でログインできないため)。

**メモ(2026-08-29)**: 本章の実装着手時に`LoginPage`の重大な不具合を発見・修正した。ログインフォーム送信後の遷移が`location.state.from`(元々アクセスしようとしていたURL)を無視して常に`navigate("/")`していたため、固定リンク経由でログインしても目的画面ではなくトップ画面に飛ばされる状態だった。`redirectTo`を`handleSubmit`でも使うよう修正し、`RequireAuth`が保持する遷移元(`location.pathname`)に正しく戻るようにした。ファイル側は`FileDetailPage`・`FilePermalinkRedirect`を新設し、掲示板と同じパターン(削除済みコンテンツへのアクセス時は404案内)で統一した。

## 10. 会議候補日時の自動抽出

- [x] 部署選択→メンバーチェックボックス一覧のUI(自分は主催者として常時選択・disabled)
- [x] free/busy計算ロジック(非表示予定もbusy扱い。`CalendarService.listBusyIntervals`を新設し、公開対象・非表示チェックに関わらず本人の全予定を対象にする)
- [x] 営業時間(平日9:00〜17:30固定)・休日除外ロジック(祝日・会社休日データを参照)
- [x] 所要時間プルダウン(30分/1時間/1時間30分/2時間)
- [x] 検索期間指定(既定2週間、終了日変更可)
- [x] 候補3件提示ロジック
- [x] コピー用テキスト生成・クリップボードコピー・コピー完了通知

**完了条件**: `docs/mockups/casemax_mockup_meeting_finder.html` の挙動が実データで再現され、コピー文面が固定テンプレート通りに生成される。→ ユニットテスト7件(空き検出、土日除外、祝日除外、他者予定との重複排除、非表示予定のbusy扱い、営業時間内判定、候補3件上限)で検証済み。フロントはモックアップのレイアウトを再現し、候補から直接カレンダー予定を作成するボタンも実装した(モックアップにのみ存在し明示的なタスク項目ではなかったが、既存のcreateEvent APIを再利用できるため追加実装)。**実SQL Serverでの最終確認は未実施**。

**メモ(2026-08-29)**: free/busy計算にはタイムゾーンの罠があった。当初UTC文字列を直接比較する実装を書きかけたが、開発機のローカルタイムゾーンがAsia/Tokyo(UTC+9)であることを確認し、営業時間(9:00-17:30)の判定はサーバーのローカル時刻(日本時間想定)で行う設計とした。テストもUTC決め打ちの期待値ではなく、ローカル時刻ベースの期待値に修正して検証した。本番サーバーのタイムゾーン設定がJSTであることが前提となる点は運用上の注意点として残る。

## 11. Googleカレンダー同期

- [ ] OAuth 2.0連携フロー(個人オプトイン)
- [ ] セカンダリカレンダー作成・管理
- [ ] 双方向同期(グループウェア→Google、Google→グループウェア)
- [ ] Webhook購読・定期更新
- [ ] 競合解決ロジック(タイムスタンプ比較)
- [ ] 会議室予約はGoogle側との双方向同期対象外とする実装上の分離
- [ ] 退職処理と連動した自動解除(予定削除→トークン失効の順序)
- [ ] トークン失効済み等のエラー時、操作ログへの記録

**完了条件**: 個人が同期をオプトインでき、双方向の変更が反映される。退職処理を実行すると同期解除が自動で完了する。会議室予約はGoogle側の直接編集の影響を受けない。

## 12. 管理設定の仕上げ

- [x] トップ画面表示件数設定(全社員共通。`/admin/top-settings`)
- [x] グループメンバー表示順設定(管理画面での並び替え。`/admin/group-order`、↑↓ボタンで並び替え後に保存)
- [x] 掲示板一括削除(最終更新日From-To指定、実行前プレビュー、操作ログ記録。`/admin/bulk-delete`)

**完了条件**: 管理者が設定した表示件数・表示順が全社員のトップ画面に反映される。一括削除が対象件数確認後にのみ実行される。→ ユニットテスト4件(表示件数保存、表示順反映、プレビューが削除を実行しないこと、実行時の操作ログ記録)で検証済み。**実SQL Serverでの最終確認は未実施**。

**メモ(2026-08-29)**: 一括削除はプレビュー(`GET /bulk-delete/preview`)と実行(`POST /bulk-delete`)を分離し、フロントは対象件数を確認するまで削除ボタンを表示しない設計にした。表示順はドラッグ&ドロップではなく↑↓ボタンでの並び替えとした(要件は「管理画面での並び替え」とのみ規定されており、ドラッグ&ドロップは明示要件ではないため、実装コストの低い方式を採用)。

## 13. バックアップ運用

- [x] 日次バックアップの設定(DB・ファイル実体。`scripts/backup/backup-files.ps1`・`backup-database.ps1`、保持日数30日での自動クリーンアップ含む)
- [x] タスクスケジューラへの自動登録スクリプト(`register-scheduled-tasks.ps1`)
- [ ] バックアップ先が本体サーバーと物理的に分離されていることの確認(実際の保存先=NAS/別サーバー等が未確定のため、スクリプト側はパラメータ化のみ完了)

**完了条件**: バックアップが日次で自動実行され、本体サーバー障害時にも復元可能な場所に保存されている。→ `backup-files.ps1`はダミーディレクトリで実機動作確認済み(ミラーコピー・`.tmp`除外・保持日数超過分の自動削除)。robocopyの成功終了コード(1)がタスクスケジューラに失敗と誤判定される不具合を発見し、`exit 0`を明示して修正した。`backup-database.ps1`は`sqlcmd`実行部分を含むため実SQL Server環境での確認は未実施。**バックアップ先(NAS等)の実際の場所とDB専用バックアップアカウントの払い出しが決まり次第、環境変数を設定するだけで運用開始できる状態。**

**メモ(2026-08-29)**: PowerShell 5.1でスクリプトファイルにUTF-8 BOM無しで日本語コメントを含めると文字化けし構文エラーになる問題を実機で発見・修正した(BOM付きUTF-8で保存)。本番サーバーへの配置時も同様の注意が必要(エディタ・展開方法によってBOMが失われないか要確認)。

## 14. 本番デプロイ(Windows Server + IIS)

- [x] `deploy/iis/frontend.web.config` の作成(静的配信+SPAフォールバックのURL Rewrite。実機未検証)
- [x] `deploy/iis/backend.web.config` の作成(ARRによる`/api/*`リバースプロキシ設定。実機未検証)
- [ ] Node.jsバックエンドをWindowsサービス化するスクリプトの作成(`deploy/service/`、方式は`docs/architecture.md`未確定事項で選定)
- [ ] 本番Windows ServerへのIIS ARR・URL Rewriteモジュールの導入確認
- [ ] 本番Windows Server ⇔ SQL Server(10.194.5.57)間のネットワーク到達性確認
- [ ] HTTPS証明書のIISへのバインド
- [ ] 本番環境向け`.env`(DB接続情報等)の配置・管理方法の確立

**完了条件**: 本番Windows Server上でフロントエンド(静的配信)・バックエンド(ARR経由のAPI)・DB接続のすべてが疎通し、HTTPSでアクセスできる。

---

## 進捗状況

0〜10章、12〜13章 完了(2026-08-29時点、いずれも実DB・実ブラウザでの最終確認は未実施)。

**保留中(2026-08-29、ユーザー判断)**:
- **11章 Googleカレンダー同期**: Google CloudのOAuthクライアントID・シークレットが未用意のため保留。用意でき次第、着手可能。
- **14章 本番デプロイ**: Node.jsのWindowsサービス化方式(node-windows/PM2/NSSM)が未確定のため保留。方式が決まり次第、`deploy/service/`にスクリプトを追加する。

両章とも外部リソース(Google Cloudプロジェクト、実際のWindows Server環境)への依存が強く、現時点ではこれ以上進められないため一時停止し、他の作業を優先する。

## 全体の完了条件

- `.steering/20260827-initial-implementation/requirements.md` の受け入れ条件をすべて満たすこと。
- リント・型チェック・テストがすべて通過すること。
- 主要な業務フロー(掲示板投稿、予定登録、ファイル共有、会議候補日抽出、Googleカレンダー連携のオプトイン)をブラウザ上で実際に操作して確認済みであること。
