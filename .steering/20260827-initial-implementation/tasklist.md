# 初回実装 タスクリスト

作成日: 2026-08-27
参照元: `.steering/20260827-initial-implementation/design.md`
ステータス: ドラフト(要承認)

進捗の見方: `[ ]` 未着手 / `[~]` 着手中 / `[x]` 完了

---

## 0. 基盤構築

- [x] リポジトリ雛形作成(`docs/repository-structure.md` 準拠のディレクトリ構成。frontend/backend/shared/scripts/docker/storageを作成)
- [x] フロントエンド初期化(Vite + React + TypeScript + Tailwind CSS v4。モックアップのカラー/Murechoフォント/角丸トークンを`src/index.css`の`@theme`に反映。`npm run build`成功を確認)
- [x] バックエンド初期化(Node.js + TypeScript + Express。`src/modules/*`の11モジュールディレクトリを作成。`npm run build`/`npm run typecheck`成功を確認)
- [x] DB接続基盤のセットアップ(`mssql`ドライバで社内SQL Server 10.194.5.57への接続を実装。`/api/health/db`で実機接続を確認済み、要件定義書通りTLS証明書は`trustServerCertificate: true`が必要と判明)
- [ ] マイグレーション基盤(`backend/src/db/migrations/`はディレクトリのみ作成済み。実スキーマは1章で対応)
- [ ] CI設定(lint・型チェック・テストの自動実行。GitHub Actions等のワークフローは未作成)

**完了条件**: 空のフロントエンド/バックエンドアプリがローカルで起動し、CIが通る状態。→ ローカル起動・ビルド・DB疎通は確認済み。CI未設定のため本章は完了扱いにしない。

**メモ(2026-08-27)**: Vite 8(rolldown版)がこの開発機のNode.js v22.11.0(要件v20.19+/v22.12+)と噛み合わずネイティブバイナリエラーが発生したため、安定版のVite 6系に固定した。バックエンドはExpressを採用(Fastifyとの比較検討はせず、チームの学習コストの低さを優先)。DBアクセスはORMを使わず`mssql`(tedious)を直接使用する方針(`docs/architecture.md`で確定)。

## 1. DBスキーマ(01〜09マイグレーション)

- [ ] 01_users_groups(USER, GROUP, USER_GROUP)
- [ ] 02_board(POST, COMMENT, ATTACHMENT、PERMALINK_SLUG含む)
- [ ] 03_calendar(CALENDAR_EVENT)
- [ ] 04_holidays(HOLIDAY)
- [ ] 05_top_page(TOP_PAGE_SETTING, GROUP_MEMBER_ORDER)
- [ ] 06_rooms(MEETING_ROOM, ROOM_RESERVATION)
- [ ] 07_files(FOLDER, FILE_ITEM, FILE_VERSION、PERMALINK_SLUG含む)
- [ ] 08_logs(OPERATION_LOG)
- [ ] 09_google_sync(GOOGLE_CALENDAR_LINK)

**完了条件**: `docs/functional-design.md` のER図と一致するスキーマがマイグレーションで再現できる。

## 2. 認証・利用者管理

- [ ] ログイン画面・セッション/認証処理
- [ ] パスワードのハッシュ化保存(再表示不可)
- [ ] 利用者CRUD(管理者限定): 氏名・メール・ログインID・社員番号・所属グループ(複数可)・権限区分
- [ ] 退職処理(論理削除、ログイン禁止化)

**完了条件**: 管理者が利用者を登録・編集・退職処理でき、一般社員はログインのみ可能。パスワードがハッシュ化されていることをDB上で確認できる。

## 3. お知らせ・掲示板

- [ ] 投稿CRUD(全社/部署別、リッチテキスト本文)
- [ ] 投稿本文のサニタイズ処理(XSS対策)
- [ ] コメント機能
- [ ] 既読管理
- [ ] 操作ログ基盤の実装(掲示板の編集/削除を記録)
- [ ] 一覧・トップ画面での抜粋表示(装飾保持)

**完了条件**: リッチテキストで投稿でき、意図しないスクリプトが実行されないこと(サニタイズテスト含む)を確認。

## 4. カレンダー(個人予定)

- [ ] 予定CRUD(繰り返し予定含む)
- [ ] 公開対象(自分/全員)の実装
- [ ] 非表示チェック(独立フラグ)の実装
- [ ] 他者予定の閲覧権限ルールの適用

**完了条件**: 公開対象と非表示の組み合わせが `functional-design.md` の表通りに動作する。

## 5. 共通休日設定

- [ ] 祝日管理画面(年度選択、一覧表示、追加・削除・修正)
- [ ] 会社休日(カレンダー機能の「全員」予定として誰でも登録可能)
- [ ] カレンダー画面での祝日+会社休日の重畳表示

**完了条件**: 管理者が祝日を年次登録でき、誰でも会社休日を登録でき、両方がカレンダー上に表示される。

## 6. トップ画面

- [ ] 週間ガントチャート(本日起点1週間、本人行+グループメンバー行)
- [ ] 表示グループ切替プルダウン
- [ ] ナビゲーション5ボタン(前週/前日/本日/翌日/翌週)、画面内一時状態としての保持
- [ ] 各利用者行下の「月表示」ボタン→個人カレンダー月表示画面への遷移
- [ ] 掲示板・ファイルウィジェット(新規登録ボタン、もっと見るリンク、最新●件表示)

**完了条件**: `docs/mockups/casemax_mockup_top.html` の挙動(ナビゲーション、表示グループ切替、新規登録導線)が実データで再現される。

## 7. 会議室予約

- [ ] 会議室マスタ(MEETING_ROOM)
- [ ] 予約CRUD、空き状況表示
- [ ] 二重予約防止ロジック
- [ ] カレンダー予定との連動表示

**完了条件**: 同一会議室・同一時間帯への重複予約がエラーになる。

## 8. ファイル共有

- [ ] フォルダ階層管理
- [ ] アップロード/ダウンロード(ローカルディスク保存、DBにメタデータ)
- [ ] バージョン管理
- [ ] 操作ログへの記録(3章の基盤を再利用)
- [ ] トップ画面ウィジェットとの連携(新規登録導線)

**完了条件**: 全社員がファイルを閲覧・編集・削除でき、操作ログに記録される。ゴミ箱機能が存在しないことを確認。

## 9. 固定リンク

- [ ] 掲示板詳細画面への固定リンクボタン・URL表示・コピー機能
- [ ] ファイル詳細画面への固定リンクボタン・URL表示・コピー機能
- [ ] 未ログイン時にリンクを開いた場合、ログイン後に目的画面へ自動遷移

**完了条件**: 固定リンクをコピーし、未ログイン状態のブラウザで開くとログイン画面経由で目的画面に到達する。

## 10. 会議候補日時の自動抽出

- [ ] 部署選択→メンバーチェックボックス一覧のUI
- [ ] free/busy計算ロジック(非表示予定もbusy扱い)
- [ ] 営業時間(平日9:00〜17:30固定)・休日除外ロジック(祝日・会社休日データを参照)
- [ ] 所要時間プルダウン(30分/1時間/1時間30分/2時間)
- [ ] 検索期間指定(既定2週間、終了日変更可)
- [ ] 候補3件提示ロジック
- [ ] コピー用テキスト生成・クリップボードコピー・コピー完了通知

**完了条件**: `docs/mockups/casemax_mockup_meeting_finder.html` の挙動が実データで再現され、コピー文面が固定テンプレート通りに生成される。

## 11. Googleカレンダー同期

- [ ] OAuth 2.0連携フロー(個人オプトイン)
- [ ] セカンダリカレンダー作成・管理
- [ ] 双方向同期(グループウェア→Google、Google→グループウェア)
- [ ] Webhook購読・定期更新
- [ ] 競合解決ロジック(タイムスタンプ比較)
- [ ] 会議室予約はGoogle側との双方向同期対象外とする実装上の分離
- [ ] 退職処理と連動した自動解除(予定削除→トークン失効の順序)
- [ ] トークン失効済み等のエラー時、操作ログへの記録

**完了条件**: 個人が同期をオプトインでき、双方向の変更が反映される。退職処理を実行すると同期解除が自動で完了する。会議室予約はGoogle側の直接編集の影響を受けない。

## 12. 管理設定の仕上げ

- [ ] トップ画面表示件数設定(全社員共通)
- [ ] グループメンバー表示順設定(管理画面での並び替え)
- [ ] 掲示板一括削除(最終更新日From-To指定、実行前プレビュー、操作ログ記録)

**完了条件**: 管理者が設定した表示件数・表示順が全社員のトップ画面に反映される。一括削除が対象件数確認後にのみ実行される。

## 13. バックアップ運用

- [ ] 日次バックアップの設定(DB・ファイル実体)
- [ ] バックアップ先が本体サーバーと物理的に分離されていることの確認

**完了条件**: バックアップが日次で自動実行され、本体サーバー障害時にも復元可能な場所に保存されている。

## 14. 本番デプロイ(Windows Server + IIS)

- [ ] `deploy/iis/frontend.web.config` の作成(静的配信+SPAフォールバックのURL Rewrite)
- [ ] `deploy/iis/backend.web.config` の作成(ARRによる`/api/*`リバースプロキシ設定)
- [ ] Node.jsバックエンドをWindowsサービス化するスクリプトの作成(`deploy/service/`、方式は`docs/architecture.md`未確定事項で選定)
- [ ] 本番Windows ServerへのIIS ARR・URL Rewriteモジュールの導入確認
- [ ] 本番Windows Server ⇔ SQL Server(10.194.5.57)間のネットワーク到達性確認
- [ ] HTTPS証明書のIISへのバインド
- [ ] 本番環境向け`.env`(DB接続情報等)の配置・管理方法の確立

**完了条件**: 本番Windows Server上でフロントエンド(静的配信)・バックエンド(ARR経由のAPI)・DB接続のすべてが疎通し、HTTPSでアクセスできる。

---

## 進捗状況

0章(基盤構築)の大部分に着手済み(2026-08-27時点)。詳細は0章の各チェック状態を参照。1章(DBスキーマ)以降は未着手。

## 全体の完了条件

- `.steering/20260827-initial-implementation/requirements.md` の受け入れ条件をすべて満たすこと。
- リント・型チェック・テストがすべて通過すること。
- 主要な業務フロー(掲示板投稿、予定登録、ファイル共有、会議候補日抽出、Googleカレンダー連携のオプトイン)をブラウザ上で実際に操作して確認済みであること。
